/**
 * Paraglide i18n messages stub
 * This is a placeholder until paraglide is properly initialized with message files
 *
 * To properly set up paraglide:
 * 1. Create message files in project.inlang/messages/ (e.g., en.json, es.json)
 * 2. Run the paraglide compiler to generate this file
 */

/**
 * Message functions
 * Each function represents a translatable message
 */
export const m = {
	/**
	 * Hello world message
	 * @param {Object} params
	 * @param {string} params.name - Name to greet
	 * @returns {string}
	 */
	hello_world: ({ name }) => `Hello ${name}!`,
};
